Fork #61 correction: revert v3.85.0.4's autosave-in-drawer — it caused an infinite "Saving…" on Create drawers (a create drawer has no auto-created draft to PATCH, since #45 skips the server-side auto-draft via the `!drawerSlug` guard). fullSaveControls drawers now keep autosave OFF (stock behavior) and rely on an explicit Save Draft button via the collection's `showSaveDraftButton`. The v3.85.0.3 publish gate is retained. v3.85.0.4 is superseded — do not use.
